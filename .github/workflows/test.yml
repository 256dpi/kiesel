on: [push, pull_request]
name: Test
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Install
        uses: actions/setup-go@v5
        with:
          go-version: 1.24
      - name: Checkout
        uses: actions/checkout@v4
      - name: Test
        run: go test ./...
